# web-dev -- 2026-04-15

Invocation: afternoon | Task: Sacred Event Planner upgrade - dashboard drill-down, button wiring, user management

## What I Did

### Task 1: Dashboard Drill-Down
- Wrapped all 4 stat cards (Upcoming Events, Total Participants, Overdue Tasks, Avg Readiness) in `<Link>` components
- Added `hover:ring-2 hover:ring-sage/40 cursor-pointer transition-all` to each stat card
- Made overdue task items, low stock alerts, and today's reminders clickable too
- Routes: events?status=scheduled, /participants, /tasks?status=pending, /events

### Task 2: Wired Up All Buttons Across 6 Pages
- **events/page.tsx**: Dropdown Edit -> /events/[id]/edit, Duplicate -> clones locally, Delete -> confirmation Dialog + Supabase delete. Table rows are clickable. Reads `?status=` from URL for initial filter.
- **participants/page.tsx**: Add -> /participants/new, Edit -> /participants/[id], Email -> mailto:, Delete -> confirmation Dialog + Supabase. Bulk email opens mailto with comma-separated addresses. Bulk delete has confirmation.
- **providers/page.tsx**: Add -> /providers/new, Edit -> /providers/[id], Contact -> mailto:, Delete -> confirmation Dialog + Supabase.
- **materials/page.tsx**: Add Material -> dialog with full form + Supabase insert. Edit -> dialog with all fields + Supabase update. Restock -> dialog with quantity input + Supabase update. Low stock alerts are clickable (open restock dialog).
- **tasks/page.tsx**: New Task -> dialog with form + Supabase insert. Checkbox toggles task status (pending/completed) + persists to Supabase. Board cards clickable -> navigate to parent event. Reads `?status=` param. Added status filter dropdown.
- **reminders/page.tsx**: New Workflow -> adds empty workflow. Clone -> duplicates workflow. Add Rule -> dialog. Delete rule -> confirmation dialog + Supabase. Switch toggles rule active state + persists.

### Task 3: User Management Rewrite
- Full Supabase-backed user loading with fallback chain (users table -> user_roles join -> fallback data)
- Edit dialog: update full_name, role, is_active via Supabase
- Password reset: calls POST /api/admin/reset-password (tries Supabase Auth admin API first, falls back to SHA-256 hash in users table)
- Invite: creates user record in Supabase users table
- Delete: confirmation dialog, disabled for super_admin
- Toggle active: Switch component, disabled for super_admin
- Inline success/error messages (no toast library needed)
- Refresh button to reload users from DB
- Created API route: src/app/api/admin/reset-password/route.ts

## Key Patterns
- Login uses Supabase Auth (signInWithPassword), NOT custom JWT despite task description
- Supabase client: `createClient()` from `@/lib/supabase/client` (browser) or `@/lib/supabase/server` (API routes)
- No toast library installed; used inline state-based messages instead
- All dialogs use shadcn Dialog component (already available)
- All deletions have confirmation dialogs with loading state
- Search params read via `useSearchParams()` for filter initialization

## Files Changed
- src/app/(dashboard)/page.tsx - dashboard stat cards clickable
- src/app/(dashboard)/events/page.tsx - full button wiring
- src/app/(dashboard)/participants/page.tsx - full button wiring
- src/app/(dashboard)/providers/page.tsx - full button wiring
- src/app/(dashboard)/materials/page.tsx - full button wiring with dialogs
- src/app/(dashboard)/tasks/page.tsx - full button wiring with dialogs
- src/app/(dashboard)/reminders/page.tsx - full button wiring with dialogs
- src/app/(dashboard)/settings/users/page.tsx - complete rewrite
- src/app/api/admin/reset-password/route.ts - NEW API route
